import { Navigation } from '../index';

const Footer = () => {

    return(
        <footer className={ "footer" }>
            <Navigation type={ "footer" }/>
        </footer>
    )
}

export default Footer;